<Window
    x:Class="Calender.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calender"
    Title="Calender">


    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <NavigationView
        x:Name="NavView"
        PaneDisplayMode="Left"
        IsSettingsVisible="False"
        IsPaneToggleButtonVisible="True"
        SelectionChanged="NavView_SelectionChanged"
        Loaded="NavView_Loaded">

        <NavigationView.MenuItems>
            <NavigationViewItem Content="Calendar" Tag="CalendarPage">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE787;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Agenda" Tag="AgendaPage">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE8A5;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <!-- Primary content frame — pages are navigated into here -->
        <Frame x:Name="ContentFrame" Background="Transparent"/>

    </NavigationView>
</Window>
